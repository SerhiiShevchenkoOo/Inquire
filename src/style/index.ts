/* eslint import/prefer-default-export: 0 */
// material
import { createMuiTheme } from '@material-ui/core/styles'

export const rootStyle = createMuiTheme({
	typography: {
		htmlFontSize: 10,
	},
})
